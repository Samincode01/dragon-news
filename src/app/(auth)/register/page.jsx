import { montserrat } from '@/app/layout';
import React from 'react';

const page = () => {
    return (
        <div className={`${montserrat.className}`}>
            register
        </div>
    );
};

export default page;
